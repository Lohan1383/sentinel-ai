{
  "name": "sentinel-v1",
  "private": true,
  "version": "1.0.0",
  "description": "Sentinel V1 - South Africa personal safety and infrastructure awareness platform",
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "dev": "pnpm -r --parallel --filter @sentinel/api --filter @sentinel/web dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test"
  }
}
